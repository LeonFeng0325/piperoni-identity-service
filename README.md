# piperoni-identity-service
This is the user identity service used by all piperoni websites and backend

## Setup instructions
```
python -m venv venv
source venv/bin/activate
pip install poetry
poetry install
```
